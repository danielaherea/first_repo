# tuple = type of data structure
# tuples cannot be changed/updated etc

coordinates = (4, 5)
# coordinates[1] = 10 TypeError: 'tuple' object does not support item assignment
print(coordinates[0])

coordinates_list = [(4,5), (6,7), (80,34)] #a list of tuples